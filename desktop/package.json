{
  "name": "deskbridge-desktop",
  "productName": "DeskBridge",
  "version": "0.3.7",
  "description": "DeskBridge desktop app — cross-screen keyboard, mouse, clipboard, file drag-drop and remote screen over a private relay.",
  "author": "DeskBridge",
  "private": true,
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.cjs"
  },
  "dependencies": {
    "lucide": "0.468.0",
    "plist": "3.1.0"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "electron": "44.3.0"
  },
  "//build": "Windows-only electron-builder config. mac/linux keep using scripts/package-*.cjs (@electron/packager) and are unaffected by this block. electron-builder is invoked on demand via `npx electron-builder` in CI, so it is intentionally not a dependency here (keeps package-lock.json in sync for `npm ci`).",
  "build": {
    "appId": "com.deskbridge.app",
    "productName": "DeskBridge",
    "directories": {
      "output": "../work/dist-win"
    },
    "files": [
      "**/*",
      "!tests/**"
    ],
    "extraResources": [
      {
        "from": "../build/deskbridge.exe",
        "to": "deskbridge.exe"
      },
      {
        "from": "../work/windows-engine/bin/deskbridge-input.exe",
        "to": "deskbridge-input.exe"
      }
    ],
    "win": {
      "target": "nsis",
      "icon": "icon.png"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "artifactName": "DeskBridge-Setup-${version}.exe"
    }
  }
}
